{% capture collection_handle %}{{ product-loop | handleize }}{% endcapture %}
{% capture url %}{% if collection_handle != "" %}/collections/{{ product-loop }}{{ product.url }}{% else %}{{ product.url }}{% endif %}{% endcapture %}


<!--
  {% if product.price_min < product.compare_at_price_min %}
    <span class="sale">Sale</span>
  {% endif %}
-->

          <figure class="related-item col-xs-4 col-sm-4 col-md-12">
             <a href="{{ url }}"><img src="{{ product.featured_image | product_img_url: 'medium' }}" alt="{{ product.title | escape  }}" class="img-responsive"></a>
         </figure>


	        
	        
{% comment %}
<!--
  <div class="image">
    <a href="{{ url }}">
      <img src="{{ product.featured_image | product_img_url: 'large' }}" alt="{{ product.title | escape  }}" class="img-responsive"/>
    </a>
  </div>

  <div class="details">
    <a href="{{ url }}" class="clearfix">
      <h4 class="title">{{ product.title }}</h4>

      <span class="price">
      {% if product.available %}
        {% if product.compare_at_price_max > product.price %}
          <del>{{ product.compare_at_price | money }}</del>
        {% endif %}
        {% if product.price_varies %}
        <small><em>from</em></small>
        {% endif %}
        {{ product.price | money }}
      {% else %}
        {{ product.price | money }} Sold Out
      {% endif %}
      </span>
    </a>

    {% if template contains 'collection' %}
   
    #################
    {% comment %}
    BB REQUIRED to handle the quick-view, you can modify this to another element
    but must have the class "quick-shop" and data-handle="{{ product.handle  }}"
    {% endcomment %}
    #################
   
    <a href ="#quick-view-container" data-handle="{{ product.handle }}" class="quick-shop">Quick Shop</a>
    {% endif %}

  </div>
</div>
-->
{% endcomment %}